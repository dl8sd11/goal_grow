class Goal {
  final int id;
  final String title;
  final DateTime startDate;
  final String description;

  Goal({
    required this.id,
    required this.title,
    required this.startDate,
    required this.description,
  });
}
